@* @page *@
@page "{id:int?}"; 
@*the above stuff does something*@
@model MyScriptureJournal.Pages_ScriptRefs.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<form>
    <p>
        <select asp-for="StandardWorks" asp-items="Model.Canon">
            <option value="">All</option>
        </select>
        Title: <input type="text" asp-for="SearchString" />
        <input type="submit" value="Filter"/>
    </p>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ScriptRef[0].Canon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ScriptRef[0].Book)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ScriptRef[0].Chapters)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ScriptRef[0].Verses)
            </th>
            @* <th>
                @Html.DisplayNameFor(model => model.ScriptRef[0].Notes)
            </th> *@
            <th>
                @Html.DisplayNameFor(model => model.ScriptRef[0].lessNotes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ScriptRef[0].CreateDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ScriptRef) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Canon)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Book)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Chapters)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Verses)
            </td>
            @* <td>
                @Html.DisplayFor(modelItem => item.Notes)
            </td> *@
            <td>
                @Html.DisplayFor(modelItem => item.lessNotes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreateDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
